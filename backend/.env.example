NODE_ENV=development
PORT=4000

DATABASE_URL="postgresql://shadow:shadow@localhost:5432/shadow_db"

# Générer des secrets forts (ex: `openssl rand -hex 64`) et NE JAMAIS les committer.
JWT_ACCESS_SECRET="change-me-access-secret"
JWT_REFRESH_SECRET="change-me-refresh-secret"
JWT_ACCESS_TTL="15m"
JWT_REFRESH_TTL_DAYS=30

CORS_ORIGIN="*"
